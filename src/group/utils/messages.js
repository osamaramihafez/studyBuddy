const generateMessage = (msg, username) => ***REMOVED***
    console.log(msg);
    return ***REMOVED***
        username,
        msg,
        createdAt: new Date().getTime()
    }
}

module.exports = ***REMOVED***
    generateMessage,
}